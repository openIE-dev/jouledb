# hello

Initialize an embedded JouleDB, define a schema, insert a couple rows, query them.

```bash
./hello.sh
```

What this shows:
- `jouledb init <path>` creates a new database
- `jouledb exec <path> "<SQL>"` runs a statement
- Pretty-printed `SELECT` output
